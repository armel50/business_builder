<div class="ui piled segment">
  <h4 class="ui header"><%=@project.name%></h4>
  <p> <%=@project.description%> </p>
</div>

<%= link_to "Edit #{@project.name}", edit_department_project_path(@project.department, @project) %>